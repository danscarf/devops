- name: Patch all things Pihole related
  hosts: all
  tasks:
    - name: Update OS packages
      apt:
        name: "*"
        state: latest
      notify:
        - reboot_pihole

  handlers:
    - name: reboot_pihole
      command: reboot